# Agent Preferences & Guidelines

## Markdown Formatting Rules

- **Blank Lines Before Lists & Block Elements**: Always ensure blank lines (line breaks) precede and follow all Markdown lists (`-`, `*`, `1.`), blockquotes (`>`), headers (`#`), code blocks (```), and tables so that Markdown linters (e.g., `markdownlint`) do not complain about missing line breaks.
